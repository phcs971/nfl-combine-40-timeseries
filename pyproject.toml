[project]
name = "nfl-combine-40-timeseries"
version = "0.1.0"
description = "Position-time series of NFL Combine 40-yard dashes, by athlete class"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "pyarrow>=16",
    "numpy>=1.26",
    "scipy>=1.13",
]

[tool.uv]
package = false
